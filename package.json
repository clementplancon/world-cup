{
  "name": "world-cup-data",
  "version": "1.0.0",
  "private": true,
  "description": "Persistent server that polls football-data.org and serves the World Cup 2026 knockout bracket (Phase 0).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "fetch": "node scripts/fetch-data.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
